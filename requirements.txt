# 核心依赖
playwright>=1.40.0
requests>=2.31.0

# 签名生成（优先使用 xhshow，若无法安装可用 execjs + sign.js 兜底）
# xhshow>=0.1.0
execjs>=1.5.1

# 数据存储
SQLAlchemy>=2.0.0

# 代理支持
pysocks>=1.7.1

# 日志
loguru>=0.7.0

# 开发工具
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
ipython>=8.0.0
